{
  "name": "@repo/root",
  "private": true,
  "version": "1.0.0",
  "description": "星球钱包 — imToken 10 周年 AI 共创 Demo",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @repo/planet-wallet dev",
    "build": "pnpm --filter @repo/planet-wallet build",
    "typecheck": "pnpm -r --if-present typecheck",
    "preview": "pnpm --filter @repo/planet-wallet preview"
  },
  "devDependencies": {
    "typescript": "catalog:build"
  },
  "pnpm": {
    "overrides": {
      "react": "19.2.6",
      "react-dom": "19.2.6"
    }
  }
}
